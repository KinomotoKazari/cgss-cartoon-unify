{
  "name": "cgss-cartoon-unify",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "sync:standalone": "node scripts/sync-standalone.mjs",
    "check:standalone": "node scripts/sync-standalone.mjs --check",
    "build:mediawiki": "node scripts/build-mediawiki.mjs",
    "check:mediawiki": "node scripts/build-mediawiki.mjs --check",
    "minify:mediawiki": "node scripts/minify-mediawiki.mjs",
    "check:mediawiki-min": "node scripts/minify-mediawiki.mjs --check",
    "package:mediawiki-bundles": "node scripts/package-mediawiki-bundles.mjs"
  },
  "devDependencies": {
    "terser": "^5.51.2"
  }
}
