{
  "name": "cgss-cartoon-unify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "sync:standalone": "node scripts/sync-standalone.mjs",
    "check:standalone": "node scripts/sync-standalone.mjs --check"
  }
}
